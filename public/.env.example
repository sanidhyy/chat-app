# Backend origin used by axios and socket.io-client.
# Must match the server PORT (default http://localhost:5000).
VITE_SERVER_URI=http://localhost:5000

# localStorage key that stores the logged-in user JSON.
# This is not a third-party API secret. Any stable string is fine.
VITE_CHAT_APP_USER=chat-app-user
